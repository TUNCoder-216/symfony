{% extends 'base.html.twig' %}

{% block title %}Quiz{% endblock %}

{% block body %}
    <h1>Quiz</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Quizid</th>
                <td>{{ quiz.quizid }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ quiz.titre }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ quiz.description }}</td>
            </tr>
            <tr>
                <th>Difficulte</th>
                <td>{{ quiz.difficulte }}</td>
            </tr>
            <tr>
                <th>Durationmin</th>
                <td>{{ quiz.durationmin }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_quiz_index') }}">back to list</a>

    <a href="{{ path('app_quiz_edit', {'quizid': quiz.quizid}) }}">edit</a>

    {{ include('quiz/_delete_form.html.twig') }}
{% endblock %}
